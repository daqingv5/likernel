<nav class="navbar">
    <div class="logo">
        {{#if @site.logo}}
            <a class="site-nav-logo" href="{{@site.url}}">
                {{@site.title}}
                {{!-- <img src="{{asset "img/logo.png"}}" alt="{{@site.title}}" /> --}}
            </a>
        {{else}}
            <a class="site-nav-logo" href="{{@site.url}}">{{@site.title}}</a>
        {{/if}}
    </div>

    <div class="menu-list">
        {{#if @site.navigation}}
            {{navigation}}
        {{/if}}
        {{!-- {{#is "post"}}
            <span class="nav-post-title {{#unless @site.logo}}dash{{/unless}}">{{post.title}}</span>
        {{/is}} --}}
    </div>

    {{!-- <div class="search">
        
    </div> --}}
</nav>